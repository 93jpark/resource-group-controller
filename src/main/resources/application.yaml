spring:
  application:
    name: project-controller
  profiles:
    active: "production"
  web:
    resources:
      add-mappings: false

server:
  forward-headers-strategy: framework

app:
  k8s:
    verify-ssl: false
    kube-config:
      mode: file
      kube-config-path: "/Users/joonwoo/.kube/config"
  aipub:
    enabled: false
    server-url: "https://k10s.cluster3.idc1.ten1010.io"
    verifying-ssl: false
    username: "testuser"
    password: "asdasdasd"
