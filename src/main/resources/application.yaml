spring:
  application:
    name: project-controller
  profiles:
    active: "production"
  web:
    resources:
      add-mappings: false

server:
  forward-headers-strategy: framework

app:
  k8s:
    verify-ssl: false
    kube-config:
      mode: IN_CLUSTER
      #kube-config-path: "/Users/joonwoo/.kube/config"
  aipub:
    enabled: false
    server-url: ""
    verifying-ssl: true
    username: ""
    password: ""
