apiVersion: project.aipub.ten1010.io/v1alpha1
kind: NodeGroup
metadata:
  name: ng1
spec:
  policy:
    daemonSet:
      allowAllDaemonSets: false
      allowedNamespaces: []
      allowedDaemonSets: []
  enableNodeSelector: false
  nodeSelector: {} # node의 label을 통해서 nodeGroup에 포함시키도록 함
  nodes:
    - node2
---
